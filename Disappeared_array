class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        res=[]
        n=len(nums)
        for i in range(0,n):
            index=abs(nums[i])-1
            if(nums[index]>0):
                nums[index]=(-nums[index])
        for i in range(0,n):
            if(nums[i]>0):
                res.append(i+1)
        return res
                
                
